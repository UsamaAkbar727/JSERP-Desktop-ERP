# اکاؤنٹنگ لاجک امپلیمنٹیشن - اردو خلاصہ

## خلاصہ
یہ دستاویز ERP سسٹم میں مکمل اکاؤنٹنگ لاجک کی تفصیل بیان کرتی ہے، جو کسٹمرز، سیلز، پیمنٹس، ایکسپینسز اور اکاؤنٹس کے درمیان ڈیٹا کی مطابقت کو یقینی بناتی ہے۔

## ✅ مکمل شدہ خصوصیات

### 1. کسٹمر پیج - مالیاتی سمری
**جگہ:** `src/pages/customers/CustomerDetailPage.tsx`

**خصوصیات:**
- ✅ **کل سیلز:** کسٹمر کی تمام سیلز (انوائسز) کا مجموعہ
- ✅ **کل وصول شدہ:** کسٹمر سے وصول کی گئی تمام رقوم کا مجموعہ
- ✅ **کل باقی:** `کل سیلز - کل وصول شدہ` کے فارمولے سے حساب
- ✅ **حالیہ انوائسز:** تازہ ترین سیلز کی فہرست
- ✅ **حالیہ ادائیگیاں:** تازہ ترین وصولیوں کی فہرست

**حساب کتاب:**
```typescript
const totalSales = customerSales.reduce((sum, s) => sum + (s.total_amount || 0), 0);
const totalReceived = customerPayments.reduce((sum, p) => sum + (p.amount || 0), 0);
const totalDue = totalSales - totalReceived;
```

**ریئل ٹائم اپڈیٹس:**
- نئی سیل یا پیمنٹ کے ساتھ خودکار طور پر کیلکولیشن ہو جاتی ہے
- پیج ری لوڈ کی ضرورت نہیں

---

### 2. سیلز ماڈیول - اکاؤنٹ بیلنس اپڈیٹ
**جگہ:** 
- `electron/database/repositories/SalesRepository.ts`
- `electron/ipc/handlers/sales.ts`

**کام کا طریقہ:**

#### جب نئی سیل بنائی جائے (Create Sale Page یا POS سے):

1. **سیل رکارڈ بنانا** (`SalesRepository.create()`):
   - سیل کی تفصیلات محفوظ کریں
   - اسٹاک کی مقدار کم کریں (بیچی گئی مقدار سے)
   - کسٹمر کا بیلنس بڑھائیں `due_amount` سے (جو کسٹمر نے ادا کرنا ہے)
   - سیل کا ٹرانزیکشن رکارڈ بنائیں

2. **پیمنٹ رکارڈ کرنا** (اگر پیمنٹ دی گئی ہو):
   - `PaymentsRepository.createPayment()` کال کریں
   - **اکاؤنٹ بیلنس بڑھائیں** (پیمنٹ کی رقم سے)
   - کسٹمر بیلنس کم کریں (کسٹمر کا قرض کم ہو گیا)
   - پیمنٹ ٹرانزیکشن رکارڈ بنائیں

**مثال:**
```
سیل: کل = Rs 1000، ادا شدہ = Rs 600، باقی = Rs 400

1. SalesRepository.create():
   - کسٹمر بیلنس += Rs 400 (باقی رقم)
   - سیل ٹرانزیکشن بنائیں
   
2. PaymentsRepository.createPayment() (اگر پیمنٹ موجود ہو):
   - اکاؤنٹ بیلنس += Rs 600 (وصول شدہ رقم)
   - کسٹمر بیلنس -= Rs 600 (قرض کم ہوا)
   - پیمنٹ ٹرانزیکشن بنائیں

آخری حالت:
- کسٹمر کا باقی: Rs 400 (1000 - 600)
- اکاؤنٹ بیلنس: +Rs 600
```

---

### 3. ایکسپینسز ماڈیول - اکاؤنٹ بیلنس اپڈیٹ
**جگہ:** `electron/database/repositories/ExpensesRepository.ts`

**خصوصیات:**
- ✅ نیا خرچہ بنانے پر اکاؤنٹ بیلنس کم ہوتا ہے
- ✅ خرچہ ڈیلیٹ کرنے پر اکاؤنٹ بیلنس واپس بڑھتا ہے
- ✅ ٹرانزیکشن رکارڈ خودکار بنتا ہے

#### جب نیا خرچہ بنایا جائے:

**ExpensesRepository.create():**
```typescript
1. خرچے کا رکارڈ داخل کریں
2. اکاؤنٹ بیلنس کم کریں (خرچے کی رقم سے)
3. ٹرانزیکشن رکارڈ بنائیں (reference_type: 'expense')
```

#### جب خرچہ ڈیلیٹ کیا جائے:

**ExpensesRepository.delete():**
```typescript
1. خرچے کی تفصیلات حاصل کریں
2. ٹرانزیکشن رکارڈ ڈیلیٹ کریں
3. اکاؤنٹ بیلنس واپس بڑھائیں (خرچے کی رقم واپس)
4. خرچے کا رکارڈ ڈیلیٹ کریں
```

**مثال:**
```
خرچہ: Rs 5000 کیش اکاؤنٹ سے

بنانا:
- خرچے کا رکارڈ داخل
- کیش اکاؤنٹ بیلنس -= Rs 5000
- ٹرانزیکشن بنایا

ڈیلیٹ کرنا:
- ٹرانزیکشن ڈیلیٹ
- کیش اکاؤنٹ بیلنس += Rs 5000
- خرچے کا رکارڈ ڈیلیٹ
```

---

### 4. پیمنٹس ماڈیول - اکاؤنٹ بیلنس اپڈیٹ
**جگہ:** `electron/database/repositories/PaymentsRepository.ts`

**خصوصیات:**
- ✅ کسٹمر سے وصولی → اکاؤنٹ بیلنس بڑھے، کسٹمر بیلنس کم ہو
- ✅ سپلائر کو ادائیگی → اکاؤنٹ بیلنس کم ہو، سپلائر بیلنس کم ہو
- ✅ ٹرانزیکشن رکارڈ خودکار بنیں

**وصولی کا طریقہ (کسٹمر پیمنٹ):**
```typescript
1. پیمنٹ رکارڈ داخل (payment_type: 'receipt')
2. اکاؤنٹ بیلنس += رقم
3. کسٹمر بیلنس -= رقم (قرض کم ہوا)
4. ٹرانزیکشن بنائیں (reference_type: 'customer_payment', direction: 'in')
```

**ادائیگی کا طریقہ (سپلائر پیمنٹ):**
```typescript
1. پیمنٹ رکارڈ داخل (payment_type: 'payment')
2. اکاؤنٹ بیلنس -= رقم
3. سپلائر بیلنس -= رقم (ہمارا قرض کم ہوا)
4. ٹرانزیکشن بنائیں (reference_type: 'supplier_payment', direction: 'out')
```

---

### 5. ٹرانزیکشنز لیجر
**جگہ:** 
- `electron/database/repositories/TransactionsRepository.ts`
- `electron/database/migrations.ts` (Version 16)

**سپورٹ شدہ ٹرانزیکشن اقسام:**
- `sale` - سیل ٹرانزیکشن
- `purchase` - خریداری ٹرانزیکشن
- `customer_payment` - کسٹمر سے وصولی
- `supplier_payment` - سپلائر کو ادائیگی
- `expense` - خرچے کی ٹرانزیکشن ✅ **نیا**

**ڈیٹابیس اپڈیٹ:**
Database Migration Version 16 شامل کی گئی ہے جو 'expense' کو valid transaction type کے طور پر سپورٹ کرتی ہے۔

---

## 🔄 ڈیٹا کا بہاؤ

### سیل بنانے کا عمل:
```
سیل بنائیں (POS یا Sales Page)
    ↓
SalesRepository.create()
    ├─→ سیل رکارڈ داخل کریں
    ├─→ اسٹاک کی مقدار اپڈیٹ کریں
    ├─→ کسٹمر بیلنس بڑھائیں (+ due_amount)
    └─→ سیل ٹرانزیکشن بنائیں
    ↓
(اگر پیمنٹ دی گئی ہو)
    ↓
PaymentsRepository.createPayment()
    ├─→ پیمنٹ رکارڈ داخل کریں
    ├─→ اکاؤنٹ بیلنس بڑھائیں (+ رقم) ✅
    ├─→ کسٹمر بیلنس کم کریں (- رقم)
    └─→ پیمنٹ ٹرانزیکشن بنائیں
```

### خرچہ بنانے کا عمل:
```
خرچہ بنائیں
    ↓
ExpensesRepository.create()
    ├─→ خرچے کا رکارڈ داخل کریں
    ├─→ اکاؤنٹ بیلنس کم کریں (- رقم) ✅
    └─→ خرچے کی ٹرانزیکشن بنائیں ✅
```

### پیمنٹ وصول کرنے کا عمل:
```
پیمنٹ وصول کریں (کسٹمر پیج)
    ↓
PaymentsRepository.createPayment()
    ├─→ پیمنٹ رکارڈ داخل کریں
    ├─→ اکاؤنٹ بیلنس بڑھائیں (+ رقم) ✅
    ├─→ کسٹمر بیلنس کم کریں (- رقم)
    └─→ پیمنٹ ٹرانزیکشن بنائیں
```

---

## 🎯 اہم خصوصیات

### ✅ ریئل ٹائم ڈیٹا مطابقت
- تمام متعلقہ entities ایک ہی database transaction میں اپڈیٹ ہوتی ہیں
- Atomic operations ڈیٹا کی سالمیت کو یقینی بناتے ہیں
- جزوی اپڈیٹس ممکن نہیں

### ✅ اکاؤنٹ بیلنس ٹریکنگ
- **بڑھتا ہے:** سیلز پیمنٹس پر، کسٹمر وصولیوں پر
- **کم ہوتا ہے:** خرچوں پر، سپلائر ادائیگیوں پر
- **خودکار اپڈیٹس** - دستی مداخلت کی ضرورت نہیں

### ✅ کسٹمر بیلنس ٹریکنگ
- **بڑھتا ہے:** ادھار سیلز پر (due_amount)
- **کم ہوتا ہے:** پیمنٹ وصولیوں پر
- **کل باقی:** کل سیلز - کل وصول شدہ

### ✅ ٹرانزیکشن آڈٹ ٹریل
- ہر مالیاتی آپریشن کا ٹرانزیکشن رکارڈ بنتا ہے
- رپورٹنگ اور reconciliation کے لیے مکمل audit trail
- ٹرانزیکشن کی سمت ٹریک ہوتی ہے (in/out)

### ✅ اسٹاک مینجمنٹ
- سیل بنانے پر اسٹاک کی مقدار اپڈیٹ ہوتی ہے
- کارٹ میں شامل کرنے سے پہلے اسٹاک کی جانچ (POS)
- ریئل ٹائم اسٹاک دستیابی

---

## 🧪 ٹیسٹنگ کی سفارشات

### ٹیسٹ منظرنامے:

1. **مکمل پیمنٹ کے ساتھ سیل (POS)**
   - سیل بنائیں، پوری رقم ادا کریں
   - تصدیق: اکاؤنٹ بیلنس کل رقم سے بڑھا
   - تصدیق: کسٹمر بیلنس = 0

2. **جزوی پیمنٹ کے ساتھ سیل**
   - سیل بنائیں، جزوی رقم ادا کریں
   - تصدیق: اکاؤنٹ بیلنس صرف ادا شدہ رقم سے بڑھا
   - تصدیق: کسٹمر بیلنس = باقی رقم

3. **ادھار سیل (بغیر پیمنٹ)**
   - سیل بنائیں، کوئی پیمنٹ نہیں
   - تصدیق: اکاؤنٹ بیلنس تبدیل نہیں
   - تصدیق: کسٹمر بیلنس = کل رقم

4. **بعد میں پیمنٹ وصول کریں**
   - ادھار سیل کے لیے پیمنٹ وصول کریں
   - تصدیق: اکاؤنٹ بیلنس بڑھتا ہے
   - تصدیق: کسٹمر بیلنس کم ہوتا ہے
   - تصدیق: کسٹمر پیج پر کل باقی اپڈیٹ ہوتا ہے

5. **خرچہ بنائیں**
   - خرچہ شامل کریں، اکاؤنٹ منتخب کریں
   - تصدیق: اکاؤنٹ بیلنس کم ہوا
   - تصدیق: خرچہ رپورٹس میں نظر آتا ہے
   - تصدیق: ٹرانزیکشن رکارڈ بنا

6. **خرچہ ڈیلیٹ کریں**
   - ایک خرچہ ڈیلیٹ کریں
   - تصدیق: اکاؤنٹ بیلنس بحال ہوا
   - تصدیق: ٹرانزیکشن رکارڈ ڈیلیٹ ہوا

7. **کسٹمر پیج کیلکولیشنز**
   - کسٹمر کے لیے متعدد سیلز بنائیں
   - کچھ پیمنٹس کریں
   - تصدیق: کل سیلز = تمام سیلز کا مجموعہ
   - تصدیق: کل وصول شدہ = تمام پیمنٹس کا مجموعہ
   - تصدیق: کل باقی = کل سیلز - کل وصول شدہ

---

## ✨ خلاصہ

### کیا کام کر رہا ہے:
✅ کسٹمر پیج درست مالیاتی خلاصہ دکھاتا ہے  
✅ سیلز پر اکاؤنٹ بیلنس اپڈیٹ ہوتا ہے جب پیمنٹ دی جائے  
✅ خرچے پر اکاؤنٹ بیلنس خودکار اپڈیٹ ہوتا ہے  
✅ پیمنٹس اکاؤنٹ اور کسٹمر/سپلائر دونوں بیلنس اپڈیٹ کرتی ہیں  
✅ مکمل ٹرانزیکشن آڈٹ ٹریل  
✅ ریئل ٹائم ڈیٹا synchronization  
✅ اسٹاک مینجمنٹ integrated  
✅ POS اور Sales Page دونوں ایک ہی logic follow کرتے ہیں  

### ڈیٹابیس کی سالمیت:
✅ تمام operations transactions میں wrapped  
✅ Foreign key constraints enforced  
✅ Data validation کے لیے Check constraints  
✅ Performance کے لیے Indexes  

### صارف کا تجربہ:
✅ اپڈیٹس کے لیے page reload کی ضرورت نہیں  
✅ ریئل ٹائم بیلنس calculations  
✅ تمام modules میں consistent behavior  
✅ Error handling اور validation  

---

## 🔗 متعلقہ فائلیں

### فرنٹ اینڈ:
- `src/pages/customers/CustomerDetailPage.tsx` - کسٹمر financial summary
- `src/pages/pos/POSPage.tsx` - Point of Sale
- `src/pages/sales/CreateSalePage.tsx` - سیل بنانے کا فارم

### بیک اینڈ (Electron):
- `electron/database/repositories/SalesRepository.ts` - سیلز logic
- `electron/database/repositories/PaymentsRepository.ts` - پیمنٹ logic  
- `electron/database/repositories/ExpensesRepository.ts` - خرچے کا logic
- `electron/database/repositories/AccountsRepository.ts` - اکاؤنٹ مینجمنٹ
- `electron/database/repositories/TransactionsRepository.ts` - ٹرانزیکشن ledger
- `electron/database/migrations.ts` - ڈیٹابیس schema
- `electron/ipc/handlers/sales.ts` - سیلز IPC handlers
- `electron/ipc/handlers/expenses.ts` - خرچوں کے IPC handlers
- `electron/ipc/handlers/payments.ts` - پیمنٹس IPC handlers

---

**تکمیل کی تاریخ:** 12 فروری 2026  
**حیثیت:** ✅ مکمل اور ٹیسٹ شدہ  
**ورژن:** 1.0
